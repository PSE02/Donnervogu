# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

# Do not delete the top three emailaccounts, they are needed for the file_creator_test and emailccounts_controller_test
hans:
  email: "hans@example.com"
  name: "Hans Mustermann"
  preferences: 
    :html: "true"
    :quote: :above
    :offline_mode: "true"
    :send_offline_mode: "true"
    :save_offline_mode: "false"
    :signature_style: "false"
    :signature: "This is just a simple signature"
  informations:
    :name:  Hans Mustermann
    :email: hans@example.com
  standard_subaccount_id: <% pid = ProfileId.new; pid.save; pid.id %>

wicki:
  email: wicky@wikinger.il
  name: Wicki
  preferences: 
    :html: false
    :quote: :above
  standard_subaccount_id: <% pid = ProfileId.new; pid.save; pid.id %>

juerg:
  email: juerg@example.com
  name: Juerg
  preferences:
    :offline_mode: "true"
    :send_offline_mode: "false"
    :save_offline_mode: "true"
  standard_subaccount_id: <% pid = ProfileId.new; pid.save; pid.id %>

max:
  email: max@example.com
  name: Max
  preferences:
    :offline_mode: "false"
    :html: "false"
    :quoting_style: :above
    :signature_style: "true"
    :signature: "Max Muster's signature"
  informations:
    :email: max@example.com
    :name: Max
  standard_subaccount_id: <% pid = ProfileId.new; pid.save; pid.id %>

